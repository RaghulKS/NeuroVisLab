# Optional advanced research stack. The code has fallbacks when these are absent.
torch>=2.2
torchvision>=0.17
opencv-python>=4.9
streamlit>=1.34
transformers>=4.40
mlflow>=2.12
shap>=0.45

# FAISS is useful for large retrieval indexes but may not have wheels for every OS.
faiss-cpu>=1.8; platform_system != "Windows" and platform_system != "Darwin"

